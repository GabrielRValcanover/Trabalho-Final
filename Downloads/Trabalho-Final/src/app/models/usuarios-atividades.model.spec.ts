import { UsuariosAtividades } from './usuarios-atividades.model';

describe('UsuariosAtividades', () => {
  it('should create an instance', () => {
    expect(new UsuariosAtividades()).toBeTruthy();
  });
});
